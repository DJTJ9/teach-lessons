# LR-0007: Eigene Notion-Datenbank mit vollständigem Schema erstellt

`notion_create_database` braucht eine Parent-Page-ID (kein Workspace-Root möglich bei internen Integrationen).
Schema mit 6 Properties angelegt: Name, Status, Priorität (select), Datum (date), Bereich (select), Notiz (rich_text).
Realer Task manuell eingetragen — DB funktioniert.

evidence: Tagesorganizer-DB in "Organizer"-Seite live erstellt, in Notion Desktop bestätigt, erster echter Task vorhanden.
