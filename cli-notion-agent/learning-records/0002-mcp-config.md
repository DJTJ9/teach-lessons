# LR-0002: MCP-Konfiguration in settings.json, Node.js als Runtime

MCP-Server werden in ~/.claude/settings.json unter "mcpServers" eingetragen.
Notion MCP-Server braucht Node.js (npx). Node.js ist bereits installiert.

evidence: Alle drei Quiz-Fragen korrekt.
