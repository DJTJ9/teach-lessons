# LR-0003: Notion-Datenbank lesen und Einträge abrufen

Claude Code kann via MCP-Tools (`notion_find` → `notion_query_data_source`) eine Notion-Datenbank
nach Name suchen und alle Einträge mit Properties abrufen — ohne Browser, ohne manuelles Copy-Paste.

evidence: Tasks-Datenbank live in dieser Session gesucht und alle 3 Einträge mit Status-Property korrekt ausgelesen.
