# LR-0005: Vollständiger bidirektionaler Workflow in zwei Stufen

Filter-Query (Status = "Not started") → Liste prüfen → gezielt updaten.
Zweistufiger Prompt verhindert ungewollte Massenänderungen.
Claude Code plant Tool-Kette automatisch aus einer Absichtsbeschreibung.

evidence: "Organizer für tägliche Aufgaben" via Filter gefunden, nach Bestätigung auf "In progress" gesetzt — alles ohne Copy-Paste.
