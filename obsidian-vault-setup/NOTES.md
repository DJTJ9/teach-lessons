# Notes

- Server: sync.thinkshark.de (CouchDB via Caddy reverse proxy), DB-Name `obsidian`, User `obsidian_sync`. Echtes Passwort NICHT hier notieren (Repo ist öffentlich via GitHub Pages) — liegt auf dem Server in `/root/filesystem-livesync/dat/config.json`.
- Windows-Setup lief bereits parallel in der Dev-Session: Plugin installiert, Setup-URI-Feld fälschlich mit Server-URL verwechselt (erwartet verschlüsselten Setup-String, nicht Klartext-URI) — Lesson zeigt stattdessen direkt den korrekten manuellen Weg über "Remote Database Configuration".
